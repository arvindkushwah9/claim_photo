<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @claim.user_id %>
</p>

<p>
  <strong>Title:</strong>
  <%= @claim.title %>
</p>

<%= link_to 'Edit', edit_claim_path(@claim) %> |
<%= link_to 'Back', claims_path %>
